	{% if form_hint %}
		<div class='page-header'>
			<h1 class="text-4xl pb-4 border-b">{{ form_hint }}</h1>
		</div>
	{% endif %}
	{% if msg %}
		<div class='alert alert-notification'>{{ msg }}</div>
	{% endif %}
	{% if error %}
		<div class='alert alert-danger clear'>
			<div class='alert-label'><strong>
					{{ error_txt }}
				</strong></div>
			<div class='alert-content'>
				<ul style="list-style-type:none">
					{{ error }}
				</ul>
			</div>
		</div>
	{% endif %}

	<!-- content in tabs -->
	<div class='content-tab-wrapper border-r border-b border-l'>
		<!-- tabs -->
		<ul class='bg-gradient-to-b from-white to-[#eef0f2] hover:from-[#fafafa] hover:to-[#e8ebee]'>
			{% loop formTab %}
				<li{% if active %} class='active'{% endif %}>
					<a href='#'
                        onclick="return ISPConfig.changeTab('{{ name }}','{{ app_module }}/{{ form_action }}')"
                        class="inline-flex px-4 py-2 bg-white border-l border-r">
						{{ title }}
					</a>
					</li>
			{% endloop %}
		</ul>

		<div id="tabbox_content" class="tab-content">
			<div class='tab-pane active fade in'>
				{% dyninclude 'content_tpl' %}
			</div>
		</div>

		<input type="hidden" name="_csrf_id" value="{{ _csrf_id }}" />
		<input type="hidden" name="_csrf_key" value="{{ _csrf_key }}" />
		<input type="hidden" name="next_tab" value="">
		<input type="hidden" name="phpsessid" value="{{ phpsessid }}">
	</div>
