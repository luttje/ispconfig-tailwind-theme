            <div class="form-group">
                <label for="username" class="col-sm-3 control-label">{{ username_txt }}</label>
                <div class="col-sm-9"><input type="text" name="username" id="username" value="{{ username }}" class="form-control" /></div></div>
                <div class="form-group">
                          <label for="passwort" class="col-sm-3 control-label">{{ password_txt }}</label>
                          <div class="col-sm-9">
          				<div class="input-group">
          					<input type="password" name="passwort" id="passwort" value="{{ passwort }}" title="{{ password_click_to_set_txt }}" class="form-control" autocomplete="off" onkeyup="pass_check(this.value);checkPassMatch('passwort','repeat_password');" />
          					<span class="input-group-btn">
          						<button class="btn btn-default" type="button" onclick="generatePassword('passwort','repeat_password');">{{ generate_password_txt }}</button>
          					</span>
          				</div>
          				</div>
                      </div>
            <div class="form-group">
                <label class="col-sm-3 control-label">{{ password_strength_txt }}</label>
                <div class="col-sm-9 checkbox">
                <div id="passBar"></div>
                <span id="passText">&nbsp;</span>
                </div>
            </div>
            <div class="form-group">
                <label for="repeat_password" class="col-sm-3 control-label">{{ repeat_password_txt }}</label>
                <div class="col-sm-9"><input type="password" name="repeat_password" id="repeat_password" value="" class="form-control" autocomplete="off" onkeyup="checkPassMatch('passwort','repeat_password');" /></div></div>
            <div class="form-group">
				<div class="col-sm-offset-3 col-sm-9">
					<div id="confirmpasswordError" style="display:none;" class="confirmpassworderror">{{ password_mismatch_txt }}</div>
					<div id="confirmpasswordOK" style="display:none;" class="confirmpasswordok">{{ password_match_txt }}</div>
				</div>
			</div>
            <div class="form-group">
              <label for="otp_type" class="col-sm-3 control-label">{{ otp_auth_txt }}</label>
              <div class="col-sm-9">
                <select name="otp_type" id="otp_type" class="form-control">
                  {{ otp_type }}
                </select>
              </div>
            </div>

            <div class="form-group">
                <label class="col-sm-3 control-label">{{ modules_txt }}</label>
                <div class="col-sm-9">
                    {{ modules }}
                </div>
            </div>
            <div class="form-group">
                <label for="startmodule" class="col-sm-3 control-label">{{ startmodule_txt }}</label>
                <div class="col-sm-9"><select name="startmodule" id="startmodule" class="form-control">
                    {{ startmodule }}
                </select></div>
            </div>
            <div class="form-group">
                <label class="col-sm-3 control-label">{{ app_theme_txt }}</label>
                <div class="col-sm-9">
                    {{ app_theme }}
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-3 control-label">{{ typ_txt }}</label>
                <div class="col-sm-9">
                    {{ typ }}
                </div>
            </div>
            <div class="form-group">
                <label class="col-sm-3 control-label">{{ active_txt }}</label>
                <div class="col-sm-9">
                    {{ active }}
                </div>
            </div>
            <div class="form-group">
                <label for="language" class="col-sm-3 control-label">{{ language_txt }}</label>
                <div class="col-sm-9"><select name="language" id="language" class="form-control flags">
                    {{ language }}
                </select></div>
            </div>
            <div class="form-group">
                <label class="col-sm-3 control-label">{{ lost_password_function_txt }}</label>
                <div class="col-sm-9">
                    {{ lost_password_function }}
                </div>
            </div>


        <input type="hidden" name="id" value="{{ id }}">

        <div class="clear"><div class="right">
            <button class="btn btn-default formbutton-success" type="button" value="{{ btn_save_txt }}" data-submit-form="pageForm" data-form-action="admin/users_edit.php">{{ btn_save_txt }}</button>
            <button class="btn btn-default formbutton-default" type="button" value="{{ btn_cancel_txt }}" data-load-content="admin/users_list.php">{{ btn_cancel_txt }}</button>
        </div></div>
