            <div class="form-group">
                <label for="server_id" class="col-sm-3 control-label">{{ server_id_txt }}</label>
                <div class="col-sm-9"><select name="server_id" id="server_id" class="form-control">
                    {{ server_id }}
                </select></div>
            </div>
            <div class="form-group">
                <label for="client_id" class="col-sm-3 control-label">{{ client_id_txt }}</label>
                <div class="col-sm-9"><select name="client_id" id="client_id" class="form-control">
                    {{ client_id }}
                </select></div>
            </div>
            <div class="form-group">
                <label for="ip_type" class="col-sm-3 control-label">{{ ip_type_txt }}</label>
                <div class="col-sm-9"><select name="ip_type" id="ip_type" class="form-control">
                    {{ ip_type }}
                </select></div>
            </div>
            <div class="form-group">
                <label for="ip_address" class="col-sm-3 control-label">{{ ip_address_txt }}</label>
                <div class="col-sm-9"><input type="text" name="ip_address" id="ip_address" value="{{ ip_address }}" class="form-control" /></div></div>
            <div class="form-group">
                <label class="col-sm-3 control-label">{{ virtualhost_txt }}</label>
                <div class="col-sm-9">
                    {{ virtualhost }}
                </div>
            </div>
            <div class="form-group">
                <label for="virtualhost_port" class="col-sm-3 control-label">{{ virtualhost_port_txt }}</label>
                <div class="col-sm-9"><input type="text" name="virtualhost_port" id="virtualhost_port" value="{{ virtualhost_port }}" class="form-control" /></div></div>
        
            
        <input type="hidden" name="id" value="{{ id }}">
            
        <div class="clear"><div class="right">
            <button class="btn btn-default formbutton-success" type="button" value="{{ btn_save_txt }}" data-submit-form="pageForm" data-form-action="admin/server_ip_edit.php">{{ btn_save_txt }}</button>
            <button class="btn btn-default formbutton-default" type="button" value="{{ btn_cancel_txt }}" data-load-content="admin/server_ip_list.php">{{ btn_cancel_txt }}</button>
        </div></div>
