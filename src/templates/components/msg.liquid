<div class="alert bg-blue-100 border border-blue-400 text-blue-700 px-4 py-3 rounded relative mb-5"
        id="{{ id }}"
        role="alert">
        <span class="block sm:inline">{{ slot }}</span>
</div>
