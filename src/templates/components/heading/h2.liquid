{% meta {
    "isChildOnly": true,
    "defaults": {
        "class": ""
    }
} %}
<h2 class="{{ class }} text-2xl my-4 dark:text-white"
    {% render '../util/merge-attributes' %}>
    {{ slot }}
</h2>
