{% meta {
    "isChildOnly": true,
    "defaults": {
        "class": ""
    }
} %}
<div class="page-header">
    <h1 class="{{ class }} text-4xl pb-4 border-b mb-4 border-gray-300 dark:text-white dark:border-slate-700"
        {% render '../util/merge-attributes' %}>
        {{ slot }}
    </h1>
</div>
