<button class='flex justify-center p-2 shadow-sm rounded self-stretch font-bold text-xs cursor-pointer hover:shadow-sm bg-gradient-to-b from-emerald-100 to-emerald-200 hover:from-emerald-50 hover:to-emerald-100'
    {% for attribute in attributes %}
    {{ attribute[0] }}="{{ attribute[1] }}"
    {% endfor %}>
    {{ slot }}
</button>
