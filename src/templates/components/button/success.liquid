<button class="flex justify-center py-2 px-4 shadow-sm rounded self-stretch font-bold text-xs cursor-pointer border hover:shadow-sm bg-gradient-to-b from-emerald-300 to-emerald-400 hover:from-emerald-200 hover:to-emerald-300"
    {% for attribute in attributes %}
    {{ attribute[0] }}="{{ attribute[1] }}"
    {% endfor %}>
    {{ slot }}
</button>
