<button class="group flex justify-center items-center min-h-10 px-4 rounded self-stretch font-bold text-xs cursor-pointer shadow hover:shadow-lg bg-gradient-to-b from-white to-[#eef0f2] hover:from-gray-600 hover:to-gray-700 hover:text-white dark:from-gray-700 dark:to-gray-800 dark:text-white dark:hover:bg-gray-600 transition-colors motion-reduce:transition-none"
    {% for attribute in attributes %}
    {{ attribute[0] }}="{{ attribute[1] }}"
    {% endfor %}>
    {{ slot }}
</button>
