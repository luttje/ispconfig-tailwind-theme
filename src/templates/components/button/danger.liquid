<button class="group flex justify-center items-center min-h-10 px-4 rounded font-bold text-xs cursor-pointer shadow hover:shadow-lg bg-gradient-to-b from-red-400 to-red-500 hover:from-rose-300 hover:to-red-400"
    {% for attribute in attributes %}
    {{ attribute[0] }}="{{ attribute[1] }}"
    {% endfor %}>
    {{ slot }}
</button>
