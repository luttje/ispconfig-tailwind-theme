<button class="flex justify-center py-2 px-4 shadow-sm rounded font-bold text-xs cursor-pointer border hover:shadow-sm bg-gradient-to-b from-rose-100 to-rose-200 hover:from-rose-50 hover:to-rose-100"
    {% for attribute in attributes %}
    {{ attribute[0] }}="{{ attribute[1] }}"
    {% endfor %}>
    {{ slot }}
</button>
