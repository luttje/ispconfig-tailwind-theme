{% meta {
    "isChildOnly": true,
    "defaults": {
        "class": "",
        "wrapperClass": ""
    }
} %}
<div class="{{ wrapperClass }} rounded-b border border-t-0 border-gray-300 dark:text-white dark:border-slate-800">
    <table class="{{ class }} table-fixed w-full m-0 p-0 bg-white dark:bg-slate-700">
