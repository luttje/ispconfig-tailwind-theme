{% meta {
    "isChildOnly": true,
    "defaults": {
        "id": "",
        "class": "",
        "options": ""
    }
} %}
<select name="{{ name }}" id="{{ id }}" class="{{ class }}"
    {% render '../util/merge-attributes' %}>
    {{ options }}
</select>
