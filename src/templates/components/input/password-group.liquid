{% render './group-start' %}
    {% render './label', name: '{{ name }}', slot: '{{ label }}' %}
    <div class="w-7/12">
        {% render './password', id: '{{ id }}', name: '{{ name }}', value: '{{ value }}' %}
    </div>
    <div class="w-2/12">
        {% render '../button/generate-password', password: '{{ name }}' %}
    </div>
{% render './group-end' %}
