<input type="{{ type }}"
    name="{{ name }}"
    id="{{ id }}"
    value="{{ value }}"
    class="w-full border rounded px-2 py-1"
    autocomplete="{{ autocomplete }}"
    onkeyup="{{ onkeyup }}" />
