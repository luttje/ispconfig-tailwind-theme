<input type="{{ type }}"
    name="{{ name }}"
    id="{{ id }}"
    value="{{ value }}"
    class="w-full h-full border rounded px-2 py-1 {{ class }}"
    autocomplete="{{ autocomplete }}"
    placeholder="{{ placeholder }}"
    onkeyup="{{ onkeyup }}" />
