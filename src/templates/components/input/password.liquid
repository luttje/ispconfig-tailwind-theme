{% render './base', type: 'password', id: '{{ id }}', name: '{{ name }}', value: '{{ value }}', autocomplete: 'on', onkeyup: "pass_check(this.value);checkPassMatch('{{ name }}','repeat_password');" %}
