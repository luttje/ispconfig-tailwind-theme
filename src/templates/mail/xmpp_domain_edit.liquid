            {% if is_admin %}
                <div class="form-group">
                    {% if edit_disabled %}
                        <label for="server_id_disabled" class="col-sm-3 control-label">{{ server_id_txt }}</label>
                        <div class="col-sm-9"><select name="server_id_disabled" id="server_id_disabled" class="form-control" disabled="disabled">
                            {{ server_id }}
                        </select></div>
                        <input type="hidden" name="server_id" value="{{ server_id_value }}" />
                        {% else %}
                            <label for="server_id" class="col-sm-3 control-label">{{ server_id_txt }}</label>
                            <div class="col-sm-9"><select name="server_id" id="server_id" class="form-control">
                                {{ server_id }}
                            </select></div>
                    {% endif %}
                </div>
                {% unless domain_option %}
                    <div class="form-group">
                        <label for="client_group_id" class="col-sm-3 control-label">{{ client_group_id_txt }}</label>
                        <div class="col-sm-9"><select name="client_group_id" id="client_group_id" class="form-control">
                            {{ client_group_id }}
                        </select></div>
                    </div>
                {% endunless %}
            {% else %}
                {% if only_one_server %}
                    <input type="hidden" id="server_id" name="server_id" value="{{ server_id_value }}" />
                {% else %}
                    <div class="form-group">
                        {% if edit_disabled %}
                            <label for="server_id_disabled" class="col-sm-3 control-label">{{ server_id_txt }}</label>
                            <div class="col-sm-9"><select name="server_id_disabled" id="server_id_disabled" class="form-control" disabled="disabled">
                                {{ client_server_id }}
                            </select></div>
                            <input type="hidden" name="server_id" value="{{ server_id_value }}" />
                        {% else %}
                            <label for="server_id" class="col-sm-3 control-label">{{ server_id_txt }}</label>
                            <div class="col-sm-9"><select name="server_id" id="server_id" class="form-control">
                                {{ client_server_id }}
                            </select></div>
                        {% endif %}
                    </div>
                {% endif %}
            {% endif %}

            {% if is_reseller %}
                {% unless domain_option %}
                    <div class="form-group">
                        <label for="client_group_id" class="col-sm-3 control-label">{{ client_group_id_txt }}</label>
                        <div class="col-sm-9"><select name="client_group_id" id="client_group_id" class="form-control">
                            {{ client_group_id }}
                        </select></div>
                    </div>
                {% endunless %}
            {% endif %}

            <div class="form-group">
                <label for="domain" class="col-sm-3 control-label">{{ domain_txt }}</label>
                {% if domain_option %}
                    <div class="col-sm-9"><select name="domain" id="domain" class="form-control">
                        {{ domain_option }}
                    </select></div>
                    {% else %}
                        <div class="col-sm-9"><input type="text" name="domain" id="domain" value="{{ domain }}" class="form-control" {% if edit_disabled %}readonly="readonly"{% endif %}/></div>{% endif %}
            </div>




<!--
management by maildomain is currently not supported
<div class="form-group">
    <label for="management_method" class="col-sm-3 control-label">{{ management_method_txt }}</label>
    <div class="col-sm-9"><select name="management_method" id="management_method" class="form-control">
        {{ management_method }}
    </select></div>
</div>
-->
<input type="hidden" name="management_method" id="management_method" value="0" />

<div id="toggle-management-normal" class="collapse">
    <!--
    Currently not supported
    div class="form-group">
        <label class="col-sm-3 control-label">{{ public_registration_txt }}</label>
        <div class="col-sm-9">
            {{ public_registration }}
        </div>
    </div-->
</div>
        <input type="hidden" name="public_registration" id="public_registration" value="n" />
<div id="toggle-registration-closed" class="collapse in">
    <div class="form-group">
        <label for="registration_url" class="col-sm-3 control-label">{{ registration_url_txt }}</label>
        <div class="col-sm-9">
            <input type="text" name="registration_url" id="registration_url" value="{{ registration_url }}" class="form-control" placeholder="{{ about_registration_url_txt }}" />
        </div>
    </div>
    <div class="form-group">
        <label class="col-sm-3 control-label">{{ registration_message_txt }}</label>
        <div class="col-sm-9"><textarea class="form-control" name="registration_message" id="registration_message" rows='3' cols='30' placeholder="{{ about_registration_message_txt }}">{{ registration_message }}</textarea></div>
    </div>
</div>

<div class="form-group">
    <label class="col-sm-3 control-label">{{ domain_admins_txt }}</label>
    <div class="col-sm-9"><textarea class="form-control" name="domain_admins" id="domain_admins" rows='3' cols='30' placeholder="admin@service.com, superuser@service.com">{{ domain_admins }}</textarea></div>
</div>


<div class="form-group">
    <label class="col-sm-3 control-label">{{ active_txt }}</label>
    <div class="col-sm-9">
        {{ active }}
    </div>
</div>

        <input type="hidden" name="id" value="{{ id }}">
        <input type="hidden" name="type" value="domain">
        <div class="clear"><div class="right">
            <button class="btn btn-default formbutton-success" type="button" value="{{ btn_save_txt }}" data-submit-form="pageForm" data-form-action="mail/xmpp_domain_edit.php">{{ btn_save_txt }}</button>
            <button class="btn btn-default formbutton-default" type="button" value="{{ btn_cancel_txt }}" data-load-content="mail/xmpp_domain_list.php">{{ btn_cancel_txt }}</button>
        </div></div>
<script language="JavaScript" type="text/javascript" src="js/xmpp_domain_registration.js"></script>
