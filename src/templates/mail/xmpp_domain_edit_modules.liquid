{% if limit_xmpp_anon == 'y' %}
    <div class="form-group">
        <label class="col-sm-3 control-label">{{ use_anon_host_txt }}</label>
        <div class="col-sm-9">
            {{ use_anon_host }}
        </div>
    </div>
{% endif %}
<div class="form-group">
    <label class="col-sm-3 control-label">{{ use_pubsub_txt }}</label>
    <div class="col-sm-9">
        {{ use_pubsub }}
    </div>
</div>
{% if limit_xmpp_vjud == 'y' %}
    <div class="form-group">
        <label class="col-sm-3 control-label">{{ use_vjud_txt }}</label>
        <div class="col-sm-9">
            {{ use_vjud }}
        </div>
    </div>
    <div class="form-group">
        <label for="vjud_opt_mode" class="col-sm-3 control-label">{{ vjud_opt_mode_txt }}</label>
        <div class="col-sm-3"><select name="vjud_opt_mode" id="vjud_opt_mode" class="form-control">
            {{ vjud_opt_mode }}
        </select></div>
    </div>
{% endif %}
{% if limit_xmpp_proxy == 'y' %}
    <div class="form-group">
        <label class="col-sm-3 control-label">{{ use_proxy_txt }}</label>
        <div class="col-sm-9">
            {{ use_proxy }}
        </div>
    </div>
{% endif %}
{% if limit_xmpp_status == 'y' %}
    <div class="form-group">
        <label class="col-sm-3 control-label">{{ use_status_host_txt }}</label>
        <div class="col-sm-9">
            {{ use_status_host }}
        </div>
    </div>
{% endif %}



        <input type="hidden" name="id" value="{{ id }}">
        <input type="hidden" name="type" value="modules" />
        {% render '../components/form-submit-buttons', cancel_load_content: 'mail/xmpp_domain_list.php', save_form_action: 'mail/xmpp_domain_edit.php' %}
