            <div class="form-group">
                <label for="server_id" class="col-sm-3 control-label">{{ server_id_txt }}</label>
                <div class="col-sm-9"><select name="server_id" id="server_id" class="form-control">
                    {{ server_id }}
                </select></div>
            </div>
            <div class="form-group">
                <label for="source" class="col-sm-3 control-label">{{ source_txt }}</label>
                <div class="col-sm-9"><input type="text" name="source" id="source" value="{{ source }}" class="form-control" /></div></div>
            <div class="form-group">
                <label for="type" class="col-sm-3 control-label">{{ type_txt }}</label>
                <div class="col-sm-9"><select name="type" id="type" class="form-control">
                    {{ type }}
                </select></div>
            </div>
            <div class="form-group">
                <label class="col-sm-3 control-label">{{ active_txt }}</label>
                <div class="col-sm-9">
                    {{ active }}
                </div>
            </div>
        

        <input type="hidden" name="id" value="{{ id }}">
        <input name="access" type="hidden" value="{{ access }}">

        {% render '../components/form-submit-buttons', cancel_load_content: 'mail/mail_blacklist_list.php', save_form_action: 'mail/mail_blacklist_edit.php' %}
