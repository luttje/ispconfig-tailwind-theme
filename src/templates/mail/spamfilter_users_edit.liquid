            <div class="form-group">
                <label for="server_id" class="col-sm-3 control-label">{{ server_id_txt }}</label>
                <div class="col-sm-9"><select name="server_id" id="server_id" class="form-control">
                    {{ server_id }}
                </select></div>
            </div>
            <div class="form-group">
                <label for="priority" class="col-sm-3 control-label">{{ priority_txt }}</label>
                <div class="col-sm-9"><select name="priority" id="priority" class="form-control">
                    {{ priority }}
                </select></div>
            </div>
            <div class="form-group">
                <label for="policy_id" class="col-sm-3 control-label">{{ policy_id_txt }}</label>
                <div class="col-sm-9"><select name="policy_id" id="policy_id" class="form-control">
                    {{ policy_id }}
                </select></div>
            </div>
            <div class="form-group">
                <label for="email" class="col-sm-3 control-label">{{ email_txt }}</label>
                <div class="col-sm-9"><input type="text" name="email" id="email" value="{{ email }}" class="form-control" /></div></div>
            <div class="form-group">
                <label for="fullname" class="col-sm-3 control-label">{{ fullname_txt }}</label>
                <div class="col-sm-9"><input type="text" name="fullname" id="fullname" value="{{ fullname }}" class="form-control" /></div></div>
            <div class="form-group">
                <label for="local" class="col-sm-3 control-label">{{ local_txt }}</label>
                <div class="col-sm-9"><select name="local" id="local" class="form-control">
                    {{ local }}
                </select></div>
            </div>
        

        <input type="hidden" name="id" value="{{ id }}">

        {% render '../components/form-submit-buttons', cancel_load_content: 'mail/spamfilter_users_list.php', save_form_action: 'mail/spamfilter_users_edit.php' %}
